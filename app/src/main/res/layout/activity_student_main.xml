<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/campus_background">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/student_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/student_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/campus_surface"
        app:itemIconTint="@color/bottom_nav_student_icon_tint"
        app:itemTextColor="@color/bottom_nav_student_text_tint"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/student_bottom_nav" />
</LinearLayout>
